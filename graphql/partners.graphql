query Partners($locale: SiteLocale,) {
  allPartners(locale: $locale) {
    title
    images {
    responsiveImage {
        alt
        base64
        bgColor
        title
        aspectRatio
        height
        sizes
        src
        width
      }
        }
    }

  }
