query PartnersPage($locale: SiteLocale) {
  partner(locale: $locale) {
    title
    images {
      responsiveImage {
        alt
        base64
        bgColor
        title
        aspectRatio
        height
        sizes
        src
        width
      }
    }
    description {
      value
    }
  }
}
