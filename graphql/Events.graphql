query NewEventQuery($locale: Locale!) {
  events(first: 1, locales: [$locale]) {
    id
    title
    new
    date
    edition
    location
    singUpLink
    lead {
      ... on Person {
        id
        name
        position
        photo(locales: en) {
          width
          height
          url
        }
      }
    }
    speakers {
      ... on Person {
        id
        name
        position
        photo(locales: en) {
          width
          height
          url
        }
      }
    }
    gallery {
      width
      height
      url
      fileName
    }
  }
}
