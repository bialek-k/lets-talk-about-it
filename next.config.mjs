/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.datocms-assets.com',
      'another-domain.com',
      'drive.google.com',
      'https://drive.google.com/',
    ],
  },
};

export default nextConfig;
